# neuro-co-xai

Convenience imports for `neuro-co-attr`, `neuro-co-probe`, and `neuro-co-cax`.

```python
from neuro_co.xai import gradient_attribution, fit_concept_probes
```

Import the underlying package directly when you only need attribution, probing,
or constraint-based explanations.
